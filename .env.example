# Template for .env — copy this file to .env and fill in real values.
# The .env file itself must never be committed.

DB_DSN=user:password@tcp(127.0.0.1:3306)/go_user_db?parseTime=true
PORT=8093
GIN_MODE=debug
